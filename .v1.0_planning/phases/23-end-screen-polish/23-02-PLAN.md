---
phase: 23-end-screen-polish
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified: []
autonomous: false
requirements: [SPRITE-PASS]
---

<objective>
Final sprite replacement pass. User provides sprite assets; Claude assigns them to the relevant GameObjects, prefabs, and UI elements.

This plan is intentionally left open — the user will provide the sprites and specify where each one goes. Claude will then assign them via Unity MCP or direct prefab/scene modification.
</objective>

<context>
User provides sprite files to Assets/Sprites/ or similar.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <what-built>
All gameplay mechanics are complete. Sprites need to be assigned.
  </what-built>
  <how-to-verify>
User provides a list of sprites and their target assignments. For example:
- "player_sprite.png → Player prefab SpriteRenderer"
- "boss1_sprite.png → P'blob SpriteRenderer"
- "victory_image.png → VictoryUI victoryImage"

Claude will then assign each sprite to the correct location.
  </how-to-verify>
  <resume-signal>Provide sprite files and assignment instructions.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Assign sprites to all specified targets</name>
  <files>
    (depends on user input)
  </files>
  <action>
For each sprite the user provides:
1. Import to Assets/Sprites/ if not already there.
2. Set texture import settings (Sprite 2D, correct pixels-per-unit).
3. Assign to the target SpriteRenderer, Image, or UI element.
4. Verify no pink/missing references remain.
  </action>
  <verify>Play through full loop — no pink squares or missing sprite warnings.</verify>
  <done>All user-provided sprites assigned to their targets. No missing references.</done>
</task>

</tasks>

<success_criteria>
All placeholder sprites replaced with user-provided final art. No missing sprite references in the full game loop.
</success_criteria>

<output>
After completion, create `.planning/phases/23-end-screen-polish/23-02-SUMMARY.md`
</output>
