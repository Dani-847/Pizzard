# Plan 1.1 Summary

**Objective**: Archive Setup & GameFlowManager Core

## Completed Tasks
- ✅ Created `Assets/Archive` folder for safely storing unused legacy scripts and assets.
- ✅ Created `GameFlowManager.cs` under `Assets/Scripts/GameFlowManager`.
  - Implemented the Singleton pattern with `DontDestroyOnLoad` for scene persistence.
  - Defined the `GameState` enum covering `MainMenu`, `IntroDialog`, `Shop`, `PreBossDialog`, and `BossFight`.
  - Added the unified `ChangeState` method to enforce strictly logged transitions.

## Verification
- Archive folder validated.
- Architecture correctly structured in code.
- Successfully committed to git.
