---
phase: 1
plan: 2
wave: 2
---

# Plan 1.2: Scene Transition Pipeline

## Objective
Establish the scene management wrapper so the GameFlowManager can trigger Unity scene loads safely when transitioning states.

## Context
- .gsd/ARCHITECTURE.md
- Assets/Scripts/GameFlowManager/GameFlowManager.cs
- Assets/Scripts/Scene/SceneLoader.cs

## Tasks

<task type="auto">
  <name>Implement SceneLoader Utility</name>
  <files>Assets/Scripts/Scene/SceneLoader.cs</files>
  <action>
    Create `SceneLoader` script handling AsyncOperation scene loading.
    Expose a public static method `LoadScene(string sceneName)` that uses `UnityEngine.SceneManagement.SceneManager` to load scenes async.
  </action>
  <verify>cmd /c type "Assets\Scripts\Scene\SceneLoader.cs"</verify>
  <done>SceneLoader.cs exists and wraps Unity's SceneManager</done>
</task>

<task type="auto">
  <name>Integrate SceneLoader into GameFlowManager</name>
  <files>Assets/Scripts/GameFlowManager/GameFlowManager.cs</files>
  <action>
    Update `GameFlowManager` to trigger `SceneLoader.LoadScene` based on the targeted `GameState` when `ChangeState` is called.
    Map states to logical scene names (e.g. "MenuScene", "ShopScene", "BossScene").
  </action>
  <verify>cmd /c findstr /C:"LoadScene" "Assets\Scripts\GameFlowManager\GameFlowManager.cs"</verify>
  <done>GameFlowManager correctly invokes SceneLoader methods on state change.</done>
</task>
