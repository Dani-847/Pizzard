---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Archive Setup & GameFlowManager Core

## Objective
Establish the clean slate by creating the Archive folder for old assets, and create the backbone GameFlowManager singleton that will control the state of the game (Intro -> Shop -> PreBoss -> Boss).

## Context
- .gsd/SPEC.md
- .gsd/ARCHITECTURE.md
- Assets/Scripts/GameFlowManager/

## Tasks

<task type="auto">
  <name>Create Archive directory</name>
  <files>Assets/Archive</files>
  <action>
    Create the `Assets/Archive` directory.
    (Note: Do not aggressively move files yet to avoid breaking references, just ensure the folder exists for future cleanup steps).
  </action>
  <verify>cmd /c dir "Assets\Archive"</verify>
  <done>Archive directory exists in the Assets folder</done>
</task>

<task type="auto">
  <name>Implement GameFlowManager Singleton</name>
  <files>Assets/Scripts/GameFlowManager/GameFlowManager.cs</files>
  <action>
    Create a robust `GameFlowManager` MonoBehaviour implementing the Singleton pattern.
    Define a `GameState` enum with values: MainMenu, IntroDialog, Shop, PreBossDialog, BossFight.
    Add a unified method `ChangeState(GameState newState)` that logs the state transition.
    Ensure the GameObject persists across scenes using `DontDestroyOnLoad`.
  </action>
  <verify>cmd /c type "Assets\Scripts\GameFlowManager\GameFlowManager.cs"</verify>
  <done>GameFlowManager.cs compiles and contains the Singleton pattern and GameState enum.</done>
</task>
