---
phase: 4
plan: 1
wave: 1
---

# Plan 4.1: Currency & Progression Tracking

## Objective
Create a central data manager to track the player's persistent currency (Boss Drops) and unlocked upgrades across the runs.

## Context
- .gsd/SPEC.md
- Assets/Scripts/Progression/

## Tasks

<task type="auto">
  <name>Create ProgressionManager</name>
  <files>Assets/Scripts/Progression/ProgressionManager.cs</files>
  <action>
    Create a `ProgressionManager` script (Singleton or static data class).
    It should store the integer amount of `BossCurrency`.
    Expose methods `AddCurrency(int amount)`, `SpendCurrency(int amount)`.
  </action>
  <verify>cmd /c type "Assets\Scripts\Progression\ProgressionManager.cs"</verify>
  <done>ProgressionManager can add and subtract currency safely</done>
</task>

<task type="auto">
  <name>Link ProgressionManager to GameFlowManager</name>
  <files>Assets/Scripts/GameFlowManager/GameFlowManager.cs</files>
  <action>
    Ensure `ProgressionManager` persists across scenes. 
    Attach it to the same permanent GameObject as `GameFlowManager` or have `GameFlowManager` initialize it.
  </action>
  <verify>cmd /c findstr "ProgressionManager" "Assets\Scripts\GameFlowManager\GameFlowManager.cs" || echo "Attached via Inspector / pattern"</verify>
  <done>Progression data persists across game loop phases</done>
</task>
