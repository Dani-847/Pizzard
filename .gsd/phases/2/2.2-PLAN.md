---
phase: 2
plan: 2
wave: 2
---

# Plan 2.2: Dialog System Framework

## Objective
Implement the foundational Dialog UI base system that will be used for both Intro and Pre-Boss dialogs. 

## Context
- .gsd/SPEC.md
- Assets/Scripts/UI/DialogController.cs

## Tasks

<task type="auto">
  <name>Implement Dialog Controller UI</name>
  <files>Assets/Scripts/UI/DialogController.cs</files>
  <action>
    Create `DialogController` script to manage displaying text sequentially.
    It should take a string array array of dialogues.
    Include a method `NextLine()` triggered by user input to advance text.
    When dialogue ends, trigger an Action callback to transition states (e.g. from Intro to Shop).
  </action>
  <verify>cmd /c type "Assets\Scripts\UI\DialogController.cs"</verify>
  <done>DialogController displays lines and triggers a callback upon completion</done>
</task>

<task type="auto">
  <name>Wire IntroDialog to GameFlowManager</name>
  <files>Assets/Scripts/GameFlowManager/IntroManager.cs</files>
  <action>
    Create `IntroManager` to handle the IntroDialog game state.
    On start, it passes the intro script to `DialogController`.
    On dialogue complete, call `GameFlowManager.Instance.ChangeState(GameState.Shop)`.
  </action>
  <verify>cmd /c type "Assets\Scripts\GameFlowManager\IntroManager.cs"</verify>
  <done>IntroManager transitions game state to Shop after dialog</done>
</task>
