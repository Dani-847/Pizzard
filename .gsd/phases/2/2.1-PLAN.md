---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Main Menu & Settings UI

## Objective
Build out the Main Menu, Settings panel, and wire them into the `GameFlowManager`.

## Context
- .gsd/SPEC.md
- Assets/Scripts/UI/
- Assets/Scripts/GameFlowManager/GameFlowManager.cs

## Tasks

<task type="auto">
  <name>Create Main Menu Controller</name>
  <files>Assets/Scripts/UI/MainMenuController.cs</files>
  <action>
    Create a `MainMenuController` script that handles UI buttons (Play, Settings, Exit).
    The Play button should call `GameFlowManager.Instance.ChangeState(GameState.IntroDialog)`.
    The Exit button should call `Application.Quit()`.
  </action>
  <verify>cmd /c type "Assets\Scripts\UI\MainMenuController.cs"</verify>
  <done>MainMenuController invokes GameFlowManager state change</done>
</task>

<task type="auto">
  <name>Create Settings Panel Controller</name>
  <files>Assets/Scripts/UI/SettingsController.cs</files>
  <action>
    Create `SettingsController` to handle basic audio/video options logic.
    Include a method to Show/Hide the panel.
    Link this logic to the `MainMenuController`'s Settings button.
  </action>
  <verify>cmd /c type "Assets\Scripts\UI\SettingsController.cs"</verify>
  <done>SettingsController logic handles UI visibility</done>
</task>
