# Architecture

> Auto-generated by /map on 2026-02-26

## Overview

This is a Unity 2D or 3D game project (likely 2D given "Sprites" and "TileMap" directories). It is a brownfield project with approximately 112 C# scripts and 13 custom shaders. The project contains multiple distinct systems for gameplay, including Elements, Attack, Items, Player, Sound, Status, UI, Weapons, Camera, and a GameFlowManager.

```
┌─────────────────────────────────────────┐
│               Unity Engine              │
├─────────────────────────────────────────┤
│    GameFlowManager & Scene Scripts      │
├─────────────────────────────────────────┤
│    Player, Elements, Weapons, Pblob     │
├─────────────────────────────────────────┤
│            Save & DataBase              │
└─────────────────────────────────────────┘
```

## Components

### Scripts/Player
- **Purpose:** Manages the main character's logic, movement, and input.
- **Location:** `Assets/Scripts/Player`

### Scripts/Elements & ElementsAttack
- **Purpose:** Likely defines elemental logic/states (e.g., fire, water) and their specific attack behaviors.
- **Location:** `Assets/Scripts/Elements` and `Assets/Scripts/ElementsAttack`

### Scripts/Weapons & Items
- **Purpose:** Handles the inventory, weapons logic, and collectible items.
- **Location:** `Assets/Scripts/Weapons` and `Assets/Scripts/Items`

### Scripts/GameFlowManager & Scene
- **Purpose:** High-level game state management, level transitions, and overall flow.
- **Location:** `Assets/Scripts/GameFlowManager` and `Assets/Scripts/Scene`

### Scripts/Save & DataBase
- **Purpose:** Persistence layer, likely handling saving/loading of game state or player progress.
- **Location:** `Assets/Scripts/Save` and `Assets/Scripts/DataBase`

### Scripts/UI & Languaje
- **Purpose:** User Interface controllers and localization logic.
- **Location:** `Assets/Scripts/UI` and `Assets/Scripts/Languaje`

## Data Flow

1. Player Input is captured and processed by `Player` scripts.
2. Actions (Attacks, Items) trigger systems in `ElementsAttack`, `Weapons`, or `Items`.
3. The `GameFlowManager` tracks overall state, while `UI` updates to reflect changes.
4. Persistent data is read from or written to using scripts in the `Save` and `DataBase` directories, likely relying on `PlayerPrefs` or `JsonUtility` (24 storage calls detected).

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Local Storage | Unity API | `PlayerPrefs` or `JsonUtility` for saving game data |

*Note: No external API or third-party backend services (like Firebase or PlayFab) were detected in the source code.*

## Technical Debt

- [ ] High volume of debug logs or TODOs. Found ~270 instances of debugging statements (e.g., `Debug.Log`, `print`, `TODO`, `FIXME`). These should be cleaned up or wrapped in conditional compilation blocks for production builds.

## Conventions

**Structure:** Traditional Unity project structure separating code into specific domain folders (`Player`, `UI`, `Camera`, `Save`). Heavy use of TextMesh Pro for UI text.
```
