---
phase: 6
plan: 1
wave: 1
---

# Plan 6.1: Boss 3 - Pomodoro Paganini (The Oven)

## Objective
Implement Pomodoro Paganini, a stationary oven boss that shoots projectiles the player must deflect back, utilizing the Player's dash.

## Context
- .gsd/SPEC.md
- Propuesta de proyecto integrado.txt
- Assets/Scripts/Bosses/PomodoroController.cs

## Tasks

<task type="auto">
  <name>Create PomodoroController</name>
  <files>Assets/Scripts/Bosses/PomodoroController.cs</files>
  <action>
    Create `PomodoroController` extending `BossBase`.
    Ensure the boss is stationary and invulnerable to standard wand attacks.
    Implement a custom `TakeDamage` override that only accepts damage from reflected specific projectiles.
  </action>
  <verify>cmd /c type "Assets\Scripts\Bosses\PomodoroController.cs"</verify>
  <done>PomodoroController handles tracking health through reflected damage only</done>
</task>

<task type="auto">
  <name>Implement Pomodoro Attacks (Pong Deflection)</name>
  <files>Assets/Scripts/Bosses/PomodoroController.cs</files>
  <action>
    In `PomodoroController`, add an Attack Coroutine that spawns projectiles.
    *(Note: The actual deflection logic will likely reside on the Projectile itself or require a hitbox interaction with the Player's Dash. For this controller, we only need to spawn them and expose the TakeDamage function properly).*
  </action>
  <verify>cmd /c findstr /C:"Attack" "Assets\Scripts\Bosses\PomodoroController.cs"</verify>
  <done>Pomodoro's attack loop for spawning deflectable projectiles is defined</done>
</task>
