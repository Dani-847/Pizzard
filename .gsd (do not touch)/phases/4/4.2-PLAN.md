---
phase: 4
plan: 2
wave: 2
---

# Plan 4.2: Shop Interface & Purchasing Logic

## Objective
Implement the Shop state UI where players can spend their Boss Currency to upgrade their Wand Tier or restore health.

## Context
- .gsd/SPEC.md
- Assets/Scripts/UI/ShopController.cs
- Assets/Scripts/GameFlowManager/ShopPhaseManager.cs

## Tasks

<task type="auto">
  <name>Create ShopController UI</name>
  <files>Assets/Scripts/UI/ShopController.cs</files>
  <action>
    Create `ShopController` handling the shop menu canvas.
    Include UI methods for `OnBuyWandUpgrade()` and `OnBuyHealth()`.
    These methods interact with `ProgressionManager` to check costs and deduct currency.
  </action>
  <verify>cmd /c type "Assets\Scripts\UI\ShopController.cs"</verify>
  <done>ShopController processes purchases with currency limits</done>
</task>

<task type="auto">
  <name>Create ShopPhaseManager</name>
  <files>Assets/Scripts/GameFlowManager/ShopPhaseManager.cs</files>
  <action>
    Create `ShopPhaseManager` that initializes the Shop scene.
    It passes references to `ShopController` (like the player's wand/health).
    Add a unified `OnLeaveShop()` method that calls `GameFlowManager.Instance.ChangeState(GameState.PreBossDialog)`.
  </action>
  <verify>cmd /c type "Assets\Scripts\GameFlowManager\ShopPhaseManager.cs"</verify>
  <done>ShopPhaseManager securely transitions to the next phase in the loop</done>
</task>
