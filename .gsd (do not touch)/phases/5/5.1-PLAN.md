---
phase: 5
plan: 1
wave: 1
---

# Plan 5.1: Boss System Foundation & Arena Manager

## Objective
Establish the base classes for Bosses and the Arena Manager that controls the BossFight GameState and handles victory bridging to the Shop.

## Context
- .gsd/SPEC.md
- Assets/Scripts/Bosses/BossBase.cs
- Assets/Scripts/GameFlowManager/BossArenaManager.cs

## Tasks

<task type="auto">
  <name>Create Boss Base Class</name>
  <files>Assets/Scripts/Bosses/BossBase.cs</files>
  <action>
    Create abstract `BossBase` MonoBehaviour.
    Include standard health tracking (`currentHealth`, `maxHealth`).
    Add an abstract `TakeDamage` method and a `Die()` method that fires an `OnBossDefeated` UnityEvent.
    The `Die()` event should grant `BossCurrency` to the `ProgressionManager`.
  </action>
  <verify>cmd /c type "Assets\Scripts\Bosses\BossBase.cs"</verify>
  <done>BossBase provides inheritance logic for all 4 bosses</done>
</task>

<task type="auto">
  <name>Create BossArenaManager</name>
  <files>Assets/Scripts/GameFlowManager/BossArenaManager.cs</files>
  <action>
    Create `BossArenaManager` for the `BossArena` scene.
    It should listen to the active boss's `OnBossDefeated` event.
    Upon victory, it should call `GameFlowManager.Instance.ChangeState(GameState.Shop)` (or IntroDialog if looping).
  </action>
  <verify>cmd /c type "Assets\Scripts\GameFlowManager\BossArenaManager.cs"</verify>
  <done>BossArenaManager controls the fight loop state transition</done>
</task>
