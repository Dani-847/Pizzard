---
phase: 3
plan: 1
wave: 1
---

# Plan 3.1: Player Core Operations

## Objective
Implement the foundation of the player character, including basic 2D movement, dodging, and a health system that takes a set number of hits.

## Context
- .gsd/SPEC.md
- Assets/Scripts/Player/
- Propuesta de proyecto integrado.txt

## Tasks

<task type="auto">
  <name>Implement PlayerMovement Controller</name>
  <files>Assets/Scripts/Player/PlayerController.cs</files>
  <action>
    Create a `PlayerController` script moving a Rigidbody2D based on Input.GetAxisRaw.
    Add a basic dash/dodge mechanic (velocity boost for a short duration) as described in the tutorial spec.
  </action>
  <verify>cmd /c type "Assets\Scripts\Player\PlayerController.cs"</verify>
  <done>PlayerController handles basic 2D movement and dodging</done>
</task>

<task type="auto">
  <name>Implement PlayerHealth System</name>
  <files>Assets/Scripts/Player/PlayerHealth.cs</files>
  <action>
    Create `PlayerHealth` to track a discrete number of hits (e.g., 3 or 5 hits before dying).
    Expose a `TakeDamage(int amount)` method that reduces hits and checks for death.
    Trigger UnityEvents on damage taken and on death for future UI integration.
  </action>
  <verify>cmd /c type "Assets\Scripts\Player\PlayerHealth.cs"</verify>
  <done>PlayerHealth manages discrete hit points and fires events</done>
</task>
