---
phase: 19-boss-2-niggel-worthington-ai
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-03-01T05:50:49.580Z
---

<current_state>
Phase 19 Plans 01 and 02 are COMPLETE (committed). Plan 03 Task 1 (scene wiring) is DONE via MCP — NiggelController is in the scene, prefabs wired, BossHealthBarUI and CoinMeterUI added. Task 2 is the human-verify checkpoint. The user ran the game from full flow (Menu → Shop → Shop → BossArena_2) and found 3 visual bugs that need fixing before the checkpoint can be approved.
</current_state>

<completed_work>

- Plan 19-01: GameBalance constants + NiggelController core — COMPLETE ✅ (commits c212ed4, eff4aaf, 56379e3)
- Plan 19-02: Attack coroutines + 4 projectile/barrier scripts — COMPLETE ✅ (commits 51bcec0, a556c99, 63fce7e)
- Plan 19-03 Task 1: Scene wiring via MCP — COMPLETE ✅ (commit d9775f9)
  - Stripped PblobController/PblobAttackPattern1-3/PblobUI/PblobRhythmManager from Boss object
  - Added NiggelController + Rigidbody2D (kinematic, gravityScale=0)
  - Wired: coinBagPrefab, healingCoinPrefab, blackDotBarrierPrefab, playerTransform, arenaCenter/ClampX/Y
  - Replaced PblobUI+DelayedHealthBar with BossHealthBarUI (healthFillBar wired to FrontHealthBar Image)
  - Created CoinMeterUI panel (gold horizontal fill bar) anchored bottom-right near mana bar
- Plan 19-03 Task 2: Human verify — BLOCKED by 3 visual bugs (see blockers)

</completed_work>

<remaining_work>

- Fix Bug 1: "P'BLOB" text showing on boss health bar — root cause FOUND: TMP text in MainMenu.unity line 2475 `m_text: P'BLOB`. This is a DontDestroyOnLoad UI element persisting from MainMenu. Need to find which GameObject this TMP is on and change the text to "Niggel" — OR wire BossHealthBarUI.bossNameText to that TMP so it updates at runtime.
- Fix Bug 2: Camera not fitting / showing empty space — CameraFollow in BossArena_2 needs to confirm its target is the Player transform. Possibly the playerTarget field is null or pointing to wrong object.
- Fix Bug 3: Coin meter (cyan bar) visible on right side but wrong position/size — CoinMeterUI was created with MCP and may have incorrect RectTransform anchoring or the Image type=Filled isn't rendering correctly. The cyan color suggests the Image color defaulted wrong.
- Plan 19-03 Task 2: Run human play-test and approve when bugs are fixed.

</remaining_work>

<decisions_made>

- Used MCP directly to wire BossArena_2 instead of NiggelArenaSetup.cs editor tool (tool failed because NiggelController wasn't in scene yet)
- BossHealthBarUI is the generic boss health bar — wired healthFillBar, left bossNameText null (should wire it to fix P'BLOB issue)
- NiggelCoinMeterUI created as new panel child of UI canvas, bottom-right anchor near ManaUI
- CoinFillBar is a child of CoinMeterUI with Image type=Filled, Horizontal, gold color (r:1, g:0.84, b:0, a:1)
- Roadmap scoped down: phases 20(Pomodoro), 21(Heckiel), 25(Tutorial), 27(Audio), 28(Credits), 29(Controller) deferred to v2.0. New phase 24 = End Screen & Polish.

</decisions_made>

<blockers>

- Bug 1 (P'BLOB text): Found in MainMenu.unity line 2475. Need to identify the exact GameObject hierarchy path to that TMP text, then either: (a) add a scene override in BossArena_2 to change the text, OR (b) wire BossHealthBarUI.bossNameText to it so it auto-updates. Option (b) is cleaner — BossHealthBarUI already has the logic to set bossNameText.text = trackedBoss.gameObject.name.
- Bug 2 (camera): CameraFollow script — need to check if `target` field is null in BossArena_2. Run `grep -n "target\|followTarget\|m_Target" Assets/Scripts/Camera/CameraFollow.cs` then check BossArena_2 scene for the CameraPivot CameraFollow component's target field.
- Bug 3 (coin meter): The cyan color on the right edge is suspicious — could be CoinFillBar Image with wrong color or position. In edit mode, open BossArena_2 and inspect CoinMeterUI in the Canvas. The Image type=Filled setting via MCP may not have applied correctly (MCP sometimes can't set enum values).

</blockers>

<context>
All three plans are functionally complete — the code is correct. These are pure scene setup / UI display bugs from the MCP wiring session. The approach for the next session is:
1. Open BossArena_2 in Unity Editor (not play mode)
2. Inspect the 3 bugs in the Scene/Game view
3. Fix P'BLOB: find the TMP GameObject path in MainMenu hierarchy, wire BossHealthBarUI.bossNameText to it via MCP
4. Fix camera: check CameraFollow.target via MCP on CameraPivot
5. Fix coin meter: check CoinMeterUI Image settings via MCP
6. Save scene, commit fixes
7. Re-enter play mode for human verify
</context>

<next_action>
Start with: Open BossArena_2.unity in Unity Editor. Then use MCP `manage_scene get_hierarchy` to scan the full UI tree and CameraPivot. Fix the 3 bugs via MCP, save scene, commit, then do the play-test for the human verify checkpoint.

Specific first command:
```
mcp manage_scene get_active  (confirm BossArena_2 is open)
mcp manage_scene get_hierarchy max_depth=3  (scan full tree)
```
Then find PblobUI/PblobBorderHealthBar TMP text and CameraFollow target.
</next_action>
