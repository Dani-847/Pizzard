---
phase: 21-spell-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Scripts/Core/GameBalance.cs
  - Assets/Scripts/Player/PlayerAimAndCast.cs
autonomous: true
requirements: [SPELL-T2-NEW, SPELL-T3-NEW, SPELL-T2-VERIFY, SPELL-T3-VERIFY]

must_haves:
  truths:
    - "All 20 new combo keys have explicit mana costs in GameBalance.Mana.SpellCosts (no silent 30f fallback)"
    - "All 20 new combo scripts can reference their GameBalance.Spells.* constants without compile error"
    - "PlayerAimAndCast.OnCastSpell dispatches all queso-placed combos and piña|pepperoni T3 variants before HandleNormalAttack"
  artifacts:
    - path: "Assets/Scripts/Core/GameBalance.cs"
      provides: "New Spells.* nested classes for all 20 combos + Casting placement distances + Mana.SpellCosts entries"
    - path: "Assets/Scripts/Player/PlayerAimAndCast.cs"
      provides: "Handle* dispatch stubs for all placed combos and piña|pepperoni T3 variants"
  key_links:
    - from: "PlayerAimAndCast.OnCastSpell"
      to: "GameBalance.Casting.*Distance"
      via: "Handle* methods using placement offsets"
      pattern: "GameBalance\\.Casting\\."
    - from: "ManaSystem.GetSpellCost"
      to: "GameBalance.Mana.SpellCosts"
      via: "dictionary lookup by key string"
      pattern: "SpellCosts"
---

<objective>
Add all prerequisite constants and dispatch stubs that every subsequent plan depends on.

Purpose: Compile errors and silent mana-cost fallbacks are the #1 pitfall for this phase. Adding all GameBalance.Spells.* nested classes, mana costs, and PlayerAimAndCast dispatch stubs first ensures every spell script written in later plans compiles on first try and costs the correct mana.

Output: Modified GameBalance.cs with 20 new constant sections. Modified PlayerAimAndCast.cs with Handle* stubs (plus dispatch entries in OnCastSpell) for all 10 placed/special-dispatch combos.
</objective>

<execution_context>
@C:/Users/danir/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-spell-completion/21-CONTEXT.md
@.planning/phases/21-spell-completion/21-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From Assets/Scripts/Core/GameBalance.cs (relevant existing structure):
```csharp
// Existing pattern for nested Spells classes:
public static class Spells
{
    public static class Queso { ... }
    public static class Pepperoni { ... }
    public static class Pina { ... }
    // T2 existing:
    public static class PiñaPiña { ... }
    // etc.
}

// Existing Mana.SpellCosts dictionary (partial):
public static class Mana
{
    public static readonly Dictionary<string, float> SpellCosts = new Dictionary<string, float>
    {
        { "queso", 10f },
        { "pepperoni", 12f },
        { "piña", 8f },
        // ... some T2 and T3 entries ...
    };
    public const float DefaultSpellCost = 30f;  // silent fallback — avoid!
}

// Existing Casting constants:
public static class Casting
{
    public const float StaticShieldDistance = 2f;
    public const float WallDistance = 2.5f;
    public const float CatapultMinDistance = 2f;
}
```

From Assets/Scripts/Player/PlayerAimAndCast.cs (current dispatch order):
```csharp
// In OnCastSpell, current order:
if (key == "queso") { HandleCheeseShield(entry); return; }
else if (key == "queso|queso|piña") { HandleStaticCheeseShield(entry); return; }
else if (key == "queso|queso|pepperoni") { HandleCheesePepperoniWall(entry); return; }
else if (key == "piña|pepperoni") { HandleTeleportAttack(entry); return; }
else if (key == "pepperoni|piña|pepperoni") { HandlePepperoniPineapplePepperoniAttack(entry); return; }
else { HandleNormalAttack(entry); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GameBalance constants for all 20 new combos</name>
  <files>Assets/Scripts/Core/GameBalance.cs</files>
  <action>
Open Assets/Scripts/Core/GameBalance.cs. Inside the Spells class, add the following new nested static classes. Add them AFTER the last existing Spells subclass. Use reasonable default values (balance tuning is deferred to Phase 23):

```csharp
// T2 Missing
public static class QuesoQueso
{
    public const float WallDuration = 6f;
    public const float WallHP = 60f;
    public const float TickDamage = 4f;
    public const float TickInterval = 0.5f;
    public const float ReflectionSpeedMultiplier = 1.2f;
    public const float ReflectionCooldown = 0.3f;
}
public static class QuesoPepperoni
{
    public const float AreaDuration = 5f;
    public const float TickDamage = 5f;
    public const float TickInterval = 0.4f;
    public const float BurnDuration = 2f;
    public const int BurnStacks = 1;
    public const float DamageScalePerSecond = 0.5f; // damage multiplier per second in area
}
public static class QuesoPina
{
    public const float PillarDuration = 5f;
    public const float TickDamage = 6f;
    public const float TickInterval = 0.5f;
    public const float Radius = 1.5f;
}
public static class PepperoniQueso
{
    public const float StickDuration = 2f;
    public const float BaseDamage = 8f;
    public const float DamagePerSecondStuck = 5f;
    public const float BurnDuration = 2f;
    public const int BurnStacks = 1;
}

// T3 piña|piña variants
public static class PinaPinaPina
{
    public const float SubExplosionRadius = 0.8f;
    public const float SubExplosionDamage = 8f;
}
public static class PinaPinaQueso
{
    // sub-projectiles are absorbing; re-use PinaPiña sub values
    public const float SubRadius = 0.5f;
    public const float SubAbsorbRadius = 1.2f;
}
public static class PinaPinaPepperoni
{
    public const float BurnDuration = 2f;
    public const int BurnStacks = 2;
}

// T3 piña|queso absorbing variants
public static class PinaQuesoPlna
{
    public const float DamagePerAbsorbed = 12f;
}
public static class PinaQuesoQueso
{
    public const float ConeRadius = 2.5f;
    public const float ConeHalfAngle = 45f;
    public const float ConeDamage = 15f;
}
public static class PinaQuesoPepperoni
{
    public const float BurnDurationPerStack = 1.5f;
}

// T3 piña|pepperoni teleport variants
public static class PinaPepperoniPina
{
    public const float ExplosionRadius = 2f;
    public const float ExplosionDamage = 20f;
}
public static class PinaPepperoniPepperoni
{
    public const float ExplosionRadius = 3f;
    public const float ExplosionDamage = 30f;
}
public static class PinaPepperoniQueso
{
    public const float ExplosionRadius = 2f;
    public const float ExplosionDamage = 20f;
    public const float ReflectionSpeedMultiplier = 1.2f;
}

// T3 queso variants — pillars
public static class QuesoPinaPina
{
    public const float Radius = 2.5f;
    public const float TickDamage = 10f;
    public const float TickInterval = 0.4f;
    public const float Duration = 5f;
}
public static class QuesoPinaQueso
{
    public const float Radius = 1.5f;
    public const float TickDamage = 6f;
    public const float TickInterval = 0.5f;
    public const float Duration = 7f;
    public const float HP = 100f;
    public const float ProjectileSlowMultiplier = 0.5f;
}
public static class QuesoPinaPepperoni
{
    public const float Radius = 1.5f;
    public const float TickDamage = 5f;
    public const float TickInterval = 0.4f;
    public const float Duration = 5f;
    public const float BurnDuration = 2f;
    public const int BurnStacksPerTick = 1;
    public const float BonusDamagePerStack = 2f;
}

// T3 queso variants — ground areas
public static class QuesoPepperoniPepperoni
{
    public const float AreaDuration = 5f;
    public const float TickDamage = 5f;
    public const float TickInterval = 0.4f;
    public const int BurnStacks = 2;
    public const float BurnDuration = 2f;
}
public static class QuesoPepperoniPina
{
    public const float AreaDuration = 5f;
    public const float TickDamage = 5f;
    public const float TickInterval = 0.4f;
    public const float BurnDuration = 2f;
    public const int BurnStacks = 1;
    public const float DoTDamage = 3f;
    public const float DoTInterval = 0.5f;
}
public static class QuesoPepperoniQueso
{
    public const float AreaDuration = 5f;
    public const float Radius = 3f;
    public const float TickDamage = 5f;
    public const float TickInterval = 0.4f;
    public const float BurnDuration = 2f;
    public const int BurnStacks = 1;
}

// T3 queso|queso|queso black hole
public static class QuesoQuesoQueso
{
    public const float AbsorbDuration = 4f;
    public const float AbsorbRadius = 3f;
    public const float ReturnDamageMultiplier = 1.5f;
    public const float ReturnProjectileSpeed = 10f;
}
```

Also inside the Casting class, add placement distance constants for the 4 new placed T2 combos and their T3 variants:

```csharp
public const float QuesoQuesoWallDistance = 2.5f;
public const float QuesoPepperoniAreaDistance = 2f;
public const float QuesoPinaPillarDistance = 2f;
public const float QuesoQuesoQuesoDistance = 2f;
```

Also add mana cost entries to GameBalance.Mana.SpellCosts dictionary for ALL 20 new combos. Find the SpellCosts dictionary and add:

```csharp
// T2 missing (if not already present)
{ "queso|queso", 22f },
{ "queso|pepperoni", 20f },
{ "queso|piña", 18f },
{ "pepperoni|queso", 20f },
// T3 piña variants
{ "piña|piña|piña", 28f },
{ "piña|piña|queso", 26f },
{ "piña|piña|pepperoni", 26f },
{ "piña|queso|piña", 28f },
{ "piña|queso|queso", 28f },
{ "piña|queso|pepperoni", 28f },
{ "piña|pepperoni|piña", 30f },
{ "piña|pepperoni|pepperoni", 32f },
{ "piña|pepperoni|queso", 30f },
// T3 queso variants
{ "queso|queso|queso", 35f },
{ "queso|piña|piña", 28f },
{ "queso|piña|queso", 30f },
{ "queso|piña|pepperoni", 30f },
{ "queso|pepperoni|pepperoni", 28f },
{ "queso|pepperoni|piña", 28f },
{ "queso|pepperoni|queso", 26f },
```

IMPORTANT: Check if any of these keys already exist in the dictionary before adding — do NOT add duplicates, only add truly missing keys.
  </action>
  <verify>Open Unity, confirm no compile errors. Check console: zero errors referencing GameBalance after domain reload.</verify>
  <done>GameBalance.cs compiles cleanly with all 20 new nested Spells classes, 4 new Casting distances, and all 20 mana cost entries added without duplicates.</done>
</task>

<task type="auto">
  <name>Task 2: Add PlayerAimAndCast dispatch stubs for placed combos and piña|pepperoni T3 variants</name>
  <files>Assets/Scripts/Player/PlayerAimAndCast.cs</files>
  <action>
Open Assets/Scripts/Player/PlayerAimAndCast.cs. In the OnCastSpell method, insert new dispatch blocks BEFORE the final `HandleNormalAttack` call. Insert in this order (after existing special cases):

```csharp
// T2 placed combos
else if (key == "queso|queso") { HandleQuesoQuesoWall(entry); return; }
else if (key == "queso|pepperoni") { HandleQuesoPepperoniArea(entry); return; }
else if (key == "queso|piña") { HandleQuesoPinaPillar(entry); return; }
// T3 queso placed combos
else if (key == "queso|queso|queso") { HandleQuesoQuesoQuesoBlackHole(entry); return; }
else if (key == "queso|piña|piña") { HandleQuesoPinaPinaPillar(entry); return; }
else if (key == "queso|piña|queso") { HandleQuesoPinaQuesoPillar(entry); return; }
else if (key == "queso|piña|pepperoni") { HandleQuesoPinaPepperoniPillar(entry); return; }
else if (key == "queso|pepperoni|pepperoni") { HandleQuesoPepperoniPepperoniArea(entry); return; }
else if (key == "queso|pepperoni|piña") { HandleQuesoPepperoniPinaArea(entry); return; }
else if (key == "queso|pepperoni|queso") { HandleQuesoPepperoniQuesoArea(entry); return; }
// T3 piña|pepperoni teleport variants
else if (key == "piña|pepperoni|piña") { HandleTeleportAttackVariant(entry, key); return; }
else if (key == "piña|pepperoni|pepperoni") { HandleTeleportAttackVariant(entry, key); return; }
else if (key == "piña|pepperoni|queso") { HandleTeleportAttackVariant(entry, key); return; }
```

Then add stub private methods at the end of the class (or near other Handle* methods). Each stub must:
1. Check entry.attackPrefab null guard
2. Compute spawn position using aim direction + GameBalance.Casting distance
3. Instantiate prefab
4. Call combiner.ClearSelectedElements()

Stub template for placed objects:
```csharp
private void HandleQuesoQuesoWall(CombinationEntry entry)
{
    if (entry.attackPrefab == null) { combiner.ClearSelectedElements(); return; }
    Vector3 aimDir = GetCurrentAimDirection();
    Vector3 spawnPos = transform.position + aimDir.normalized * Pizzard.Core.GameBalance.Casting.QuesoQuesoWallDistance;
    Instantiate(entry.attackPrefab, spawnPos, Quaternion.identity);
    combiner.ClearSelectedElements();
}

private void HandleQuesoPepperoniArea(CombinationEntry entry)
{
    if (entry.attackPrefab == null) { combiner.ClearSelectedElements(); return; }
    Vector3 aimDir = GetCurrentAimDirection();
    Vector3 spawnPos = transform.position + aimDir.normalized * Pizzard.Core.GameBalance.Casting.QuesoPepperoniAreaDistance;
    Instantiate(entry.attackPrefab, spawnPos, Quaternion.identity);
    combiner.ClearSelectedElements();
}

private void HandleQuesoPinaPillar(CombinationEntry entry)
{
    if (entry.attackPrefab == null) { combiner.ClearSelectedElements(); return; }
    Vector3 aimDir = GetCurrentAimDirection();
    Vector3 spawnPos = transform.position + aimDir.normalized * Pizzard.Core.GameBalance.Casting.QuesoPinaPillarDistance;
    Instantiate(entry.attackPrefab, spawnPos, Quaternion.identity);
    combiner.ClearSelectedElements();
}

private void HandleQuesoQuesoQuesoBlackHole(CombinationEntry entry)
{
    if (entry.attackPrefab == null) { combiner.ClearSelectedElements(); return; }
    Vector3 aimDir = GetCurrentAimDirection();
    Vector3 spawnPos = transform.position + aimDir.normalized * Pizzard.Core.GameBalance.Casting.QuesoQuesoQuesoDistance;
    Instantiate(entry.attackPrefab, spawnPos, Quaternion.identity);
    combiner.ClearSelectedElements();
}
```

For T3 pillar variants, use `Pizzard.Core.GameBalance.Casting.QuesoPinaPillarDistance` in the same pattern. For T3 area variants, use `QuesoPepperoniAreaDistance`.

For piña|pepperoni teleport variants, add one shared method:
```csharp
private void HandleTeleportAttackVariant(CombinationEntry entry, string key)
{
    if (entry.attackPrefab == null) { combiner.ClearSelectedElements(); return; }
    // Same pattern as HandleTeleportAttack — look at that method and replicate for variants
    // The prefab's script handles the actual teleport+explosion behavior
    var attack = Instantiate(entry.attackPrefab, transform.position, Quaternion.identity);
    var component = attack.GetComponent<PineapplePepperoniAttack>();
    if (component != null) component.Initialize(transform, this);
    combiner.ClearSelectedElements();
}
```

NOTE: If `PineapplePepperoniAttack` is not the correct base type name, look at what `HandleTeleportAttack` uses and replicate exactly. The goal is that piña|pepperoni|* variants go through the same initialization path as piña|pepperoni — the prefab script differences (bigger explosion, reflection) are handled in those scripts, not here.
  </action>
  <verify>Unity compiles without errors. In editor, verify OnCastSpell has all 13 new dispatch lines. The Handle* stub methods exist at the bottom of the class.</verify>
  <done>PlayerAimAndCast.cs compiles cleanly. All 10 placed-object combos and 3 piña|pepperoni T3 variants have dispatch entries in OnCastSpell. All corresponding Handle* methods exist (even if they instantiate null prefabs, the dispatch is wired).</done>
</task>

</tasks>

<verification>
- Unity console shows zero compile errors after domain reload
- GameBalance.cs: search for "QuesoQuesoQueso" — class exists
- GameBalance.Mana.SpellCosts: search for "queso|queso" — entry present with value 22f
- PlayerAimAndCast.cs: search for "queso|queso|queso" — dispatch line present
- PlayerAimAndCast.cs: search for "piña|pepperoni|piña" — dispatch line present
</verification>

<success_criteria>
All 20 new combo keys registered in SpellCosts. All 20 new Spells.* nested classes added. All 4 new Casting distances added. All 10 placed-combo + 3 piña|pepperoni T3 dispatch blocks + Handle* stubs added to PlayerAimAndCast. Zero compile errors.
</success_criteria>

<output>
After completion, create `.planning/phases/21-spell-completion/21-01-SUMMARY.md`
</output>
